{% extends 'backend/achat/index.html.twig' %}
{% block title %} les produits {% endblock  %}

{% block content %}

    <s9ection class="section">
    <div class="container-fluid">
    <div class="row">
    <div class="col-md-8 col-md-offset-2">
    <div class="panel">
    <div class="panel-heading">
        <div class="panel-title">
            <h4 style="text-align: center">Liste des produits</h4>
        </div>
    </div>

    <div class="panel-body p-20 overflow-x-auto">
    <table class="table table-striped">


    <table id="example" class="display table table-striped table-bordered" cellspacing="0" width="100%">
        <thead>
        <tr>
            <th>Reference</th>
            <th>Libelle</th>
            <th>Classe</th>
            <th>Actions</th>
        </tr>
        </thead>
        <tbody>
        {% for produit in produits %}
            <tr>
                <td><a href="{{ path('produit_show', { 'reference': produit.reference }) }}">{{ produit.reference }}</a></td>
                <td>{{ produit.libelle }}</td>
                <td>{{ produit.classe }}</td>
                <td>
                    <a href="{{ path('produit_show', { 'reference': produit.reference }) }}"><button type="button" class="btn btn-info btn-wide">show</button></a>
                    <br>
                    <a href="{{ path('produit_edit', { 'reference': produit.reference }) }}"><button type="button" class="btn btn-info btn-wide">edit</button></a>
                </td>
            </tr>
        {% endfor %}
        </tfoot>
    </table>
    <a href="{{ path('produit_new') }}"><button type="button" class="btn btn-info btn-wide">Create a new produit</button></a>





{% endblock %}