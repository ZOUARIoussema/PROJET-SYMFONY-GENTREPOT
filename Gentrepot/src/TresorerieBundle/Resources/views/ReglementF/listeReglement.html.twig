{% extends 'backend/AgentCaisse/indexAgentCaisse.html.twig' %}
{% block title %} table {% endblock %}

{% block content %}

    <h2>liste des reglements fournisseur par espèce  </h2>

    <table border="1" class="display table table-bordered table-striped" cellspacing="0" width="100%">
        <tr>
            <th>montant</th>
            <th>Date creation</th>
            <th>facture</th>
            <th>Action</th>

        </tr>
        {%for r in reglemente %}  <tr>
            <td>{{r.montant}}</td>
            <td>{{r.dateCreation|date('Y-m-d')}}</td>
            <td>{{r.facture.numeroF}}</td>

            {% if ("now"|date('Y-m-d') ==  r.dateCreation|date('Y-m-d') ) %}


            <td><a class="btn btn-success btn-xs btn-labeled" href="{{ path('tresorerie_modifierRE',{'id':r.id}) }}">modifier<span class="btn-label btn-label-right"><i class="fa fa-pencil"></i></span></a>
            <a  class="btn btn-danger btn-xs btn-labeled" href="{{ path('tresorerie_supprimerRE',{'id':r.id}) }}">supprimer<span class="btn-label btn-label-right"><i class="fa fa-times"></i></span></a> </td>


            {% endif %}




        </tr>    {% endfor %}



    </table>


    <a class="label label-success label-bordered" href="{{ path('tresorerie_ajouterREspece') }}">Ajouter reglement en espece</a>


    <h2>liste des reglement fournisseur par chèque  </h2>

    <table border="1" class="display table table-bordered table-striped" cellspacing="0" width="100%">
        <tr>
            <th>montant</th>
            <th>Date creation</th>
            <th>numero cheque</th>
            <th>Date cheque</th>
           <th>facture</th>
            <th>Action</th>

        </tr>
        {%for r in reglementc %}  <tr>
            <td>{{r.montant}}</td>
            <td>{{r.dateCreation|date('Y-m-d')}}</td>
            <td>{{r.numeroCheque}}</td>
            <td>{{r.dateCheque|date('Y-m-d')}}</td>
            <td>{{r.facture.numeroF}}</td>



            {% if ("now"|date('Y-m-d') ==  r.dateCreation|date('Y-m-d') ) %}

            <td><a class="btn btn-success btn-xs btn-labeled" href="{{ path('tresorerie_modifierRC',{'id':r.id}) }}">modifier<span class="btn-label btn-label-right"><i class="fa fa-pencil"></i></span></a>
            <a class="btn btn-danger btn-xs btn-labeled"  href="{{ path('tresorerie_supprimerRC',{'id':r.id}) }}">supprimer<span class="btn-label btn-label-right"><i class="fa fa-times"></i></span></a> </td>


            {% endif %}




        </tr>    {% endfor %}

    </table>

    <a class="label label-success label-bordered" href="{{ path('tresorerie_ajouterRCheque') }}">Ajouter reglement en cheque</a>


{% endblock %}